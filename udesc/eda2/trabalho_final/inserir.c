#include "avl.c"
#include "avl.h"
#include <time.h>
int main(int argc) {
    int chave = agrc;

    clock_t t1 = clock();
    while (chave--) {
        random
    }
    clock_t t2 = clock();
    float diff = t2 - t1;
}
