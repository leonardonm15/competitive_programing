#include <bits/stdc++.h>

using namespace std;

#define endl '\n' 
#define int long long

void solve () {
    // pra cada nodo na arvore, maior distancia dele pra baixo dele
    // binary lifting com sparse table de maximo ? podendo subir k caras
    //
    // query em log
}

signed main() {
    ios_base::sync_with_stdio(0);cin.tie(0);
    int TC = 1;
    if (TC){
        cin >> TC;
        while (TC--) solve();
    } else {
        solve();
    }
    return 0;
}
